# OpenAI API Configuration (for chat model)
OPENAI_API_KEY=your_openai_api_key_here

# Mistral API Configuration (for OCR and preprocessing - optional)
MISTRAL_API_KEY=your_mistral_api_key_here

# Tavily API Configuration (for web search - optional)
TAVILY_API_KEY=your_tavily_api_key_here

# Deployment Configuration (for Render)
# ALLOWED_ORIGINS=https://your-app.netlify.app
# PORT is automatically set by Render, no need to configure

# Optional: Override defaults
# OPENAI_MODEL=gpt-4o-mini
# EMBEDDING_MODEL=all-MiniLM-L6-v2
# CHROMA_PERSIST_DIRECTORY=./chroma_db
# CHROMA_COLLECTION_NAME=pdf_documents
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200
# TOP_K_RETRIEVAL=5
# API_HOST=127.0.0.1 (use 0.0.0.0 for production)
# API_PORT=8000 (Render provides PORT automatically)
# CHART_OUTPUT_DIR=./charts

