[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt", "python download_models.py"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python run.py"

